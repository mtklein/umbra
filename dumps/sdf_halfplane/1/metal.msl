#include <metal_stdlib>
using namespace metal;

struct meta { uint w, x0, y0, count0, count1, count2, stride0, stride1, stride2; };

kernel void main0(
    constant meta &m [[buffer(0)]],
    device ushort * __restrict p0 [[buffer(1)]],
    device const uint * __restrict p1 [[buffer(2)]],
    device const uint * __restrict p2 [[buffer(3)]],
    uint2 pos [[thread_position_in_grid]]
) {
    if (pos.x >= m.w) return;
    uint x = m.x0 + pos.x;
    uint y = m.y0 + pos.y;
    uint v0 = 0u;
    uint v1 = p1[0];
    uint v2 = p1[1];
    uint v3 = p1[2];
    uint v4 = p1[3];
    uint v5 = 1065353216u;
    uint v6 = 1u;
    uint v7 = 2u;
    uint v8 = p2[0];
    uint v9 = p2[1];
    uint v10 = p2[2];
    uint v11 = m.y0 + pos.y;
    float v12 = (float)(int)v11;
    float v13 = fma(as_type<float>(v4), v12, as_type<float>(v2));
    float v14 = fma(1.0f, v12, as_type<float>(v5));
    float v15 = fma(as_type<float>(v4), v14, as_type<float>(v2));
    float v16 = fma(v13, as_type<float>(v9), 0.0f);
    float v17 = fma(v15, as_type<float>(v9), 0.0f);
    float v18 = min(v16, v17);
    float v19 = max(v16, v17);
    uint v20 = m.x0 + pos.x;
    float v21 = (float)(int)v20;
    float v22 = fma(1.0f, v21, as_type<float>(v5));
    float v23 = fma(as_type<float>(v3), v22, as_type<float>(v1));
    float v24 = fma(v23, as_type<float>(v8), 0.0f);
    float v25 = fma(as_type<float>(v3), v21, as_type<float>(v1));
    float v26 = fma(v25, as_type<float>(v8), 0.0f);
    float v27 = max(v26, v24);
    float v28 = fma(1.0f, v19, v27);
    float v29 = fma(-1.0f, as_type<float>(v10), v28);
    uint v30 = v29 <  as_type<float>(v0) ? 0xffffffffu : 0u;
    float v31 = min(v26, v24);
    float v32 = fma(1.0f, v18, v31);
    float v33 = fma(-1.0f, as_type<float>(v10), v32);
    uint v34 = v33 <  as_type<float>(v0) ? 0xffffffffu : 0u;
    uint v35 = (v34 != 0u) ? v6 : v0;
    uint v36 = (v30 != 0u) ? v7 : v35;
    uint v37 = (uint)(ushort)v36;
    p0[y * m.stride0 + x] = (ushort)v37;
}
