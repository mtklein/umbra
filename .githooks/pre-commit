#!/bin/sh
if ! ninja; then
    echo "pre-commit: ninja failed, commit rejected"
    exit 1
fi
if ! git diff --quiet; then
    echo "pre-commit: unstaged changes remain after ninja"
    git diff --stat
    echo "Did you forget to 'git add' updated dumps?"
    exit 1
fi
