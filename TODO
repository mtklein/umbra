- Flush after each curve in slug is a hack working around some sort of Metal bug.
- Flush after the curves before the final draw in slug is also a hack working
  around lack of some API that allows that winding to stay on the GPU completely.
